---
description: Swift build verification instructions for Project2501
globs: "**/*.swift"
alwaysApply: false
---

# Building Project2501Core

The xcode workspace has pre-existing build failures in external dependencies (`mlx-swift-lm`, `IkigaJSON`). Never use `xcodebuild` to verify changes — it will always fail on those deps and waste tokens.

Instead, compile only the Project2501Core package sources (no linking) to verify your changes:

```bash
cd /Users/mariocuadra/CuadraLabs/2501-OSX/Packages/Project2501Core && swift build 2>&1 | grep -E "error:" | grep -v "IkigaJSON"
```

If the filtered output is empty, your code compiles cleanly.